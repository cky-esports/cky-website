import { Landing } from 'client-pages';

// eslint-disable-next-line import/prefer-default-export
export const ROUTES = [
  {
    path: '/',
    component: Landing,
    exact: true,
    label: 'Home',
  },
];
